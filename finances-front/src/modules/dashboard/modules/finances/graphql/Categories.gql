# import * from './Operation.gql'

query Categories (
  $operation: Operation
  ) {
  categories(operation: $operations) {
    id
    description
    operation
    user {
      id
      name
      email
    }
  }
}